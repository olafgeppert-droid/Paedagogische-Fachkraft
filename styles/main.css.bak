:root {
    --primary-color: #2563eb;       /* Kräftiges Blau statt #4a6fa5 */
    --secondary-color: #1d4ed8;     /* Dunkleres Blau statt #166088 */
    --background-color: #ffffff;
    --sidebar-bg: #c2c2c2;          /* Dunkleres Grau für besseren Kontrast */
    --sidebar-text: #000000;         /* Schwarzer Text für besseren Kontrast */
    --sidebar-hover: #94a3b8;       /* Hellerer Hover für besseren Kontrast */
    --card-bg: #f8fafc;             /* Mittelgrau statt reinweiß */
    --text-color: #1f2937;          /* Dunkleres Grau statt #2c3e50 */
    --text-light: #6b7280;          /* Mittleres Grau statt #7f8c8d */
    --border-color: #d1d5db;        /* Deutlichere Kanten statt #bdc3c7 */
    --success-color: #059669;       /* Kräftiges Grün statt #27ae60 */
    --danger-color: #dc2626;        /* Kräftiges Rot statt #e74c3c */
    --warning-color: #d97706;       /* Kräftiges Orange statt #f39c12 */
    --info-color: #2563eb;          /* Konsistentes Blau statt #3498db */
    --font-size: 15px;              /* Etwas größere Schrift */
    --input-font-size: 15px;
    --header-height: auto;
    --toolbar-height: auto;         /* Etwas höher für bessere Sichtbarkeit */
    --sidebar-width: 300px;
    --border-radius: 8px;           /* Abgerundetere Ecken */
    --shadow: 0 4px 12px rgba(0, 0, 0, 0.15); /* Stärkerer Schatten */
    --transition: all 0.3s ease;
}

[data-theme="dark"] {
    --primary-color: #3b82f6;
    --secondary-color: #2563eb;
    --background-color: #111827;
    --sidebar-bg: #1f2937;
    --sidebar-text: #f3f4f6;
    --sidebar-hover: #374151;
    --card-bg: #1f2937;
    --text-color: #f9fafb;
    --text-light: #9ca3af;
    --border-color: #374151;
    --success-color: #10b981;
    --danger-color: #ef4444;
    --warning-color: #f59e0b;
    --info-color: #3b82f6;
}

[data-theme="colored"] {
    /* Farbiges Theme mit besserem Kontrast */
    --primary-color: #dc2626;       /* Kräftiges Rot statt #ff6f61 */
    --secondary-color: #7c3aed;     /* Kräftiges Violett statt #6b5b95 */
    --background-color: #fef7ed;    /* Hellerer Hintergrund */
    --sidebar-bg: #fed7aa;          /* Helleres Orange */
    --sidebar-text: #431407;        /* Sehr dunkler Text für Kontrast */
    --sidebar-hover: #fdba74;
    --card-bg: #ffffff;
    --text-color: #1f2937;
    --text-light: #6b7280;
    --border-color: #e5e7eb;
    --success-color: #16a34a;
    --danger-color: #fecaca;
    --warning-color: #f59e0b;
    --info-color: #2563eb;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
    background-color: var(--background-color);
    color: var(--text-color);
    font-size: var(--font-size);
    line-height: 1.6;
    height: 100vh;
    overflow: hidden;
    font-weight: 500; /* Etwas dickere Schrift für bessere Lesbarkeit */
}

.app {
    display: grid;
    grid-template-areas:
        "header header"
        "toolbar toolbar"
        "nav main";
    grid-template-rows: var(--header-height) var(--toolbar-height) 1fr;
    grid-template-columns: var(--sidebar-width) 1fr;
    height: 100vh;
}

/* Header Styles */
.header {
    grid-area: header;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: white;
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    box-shadow: var(--shadow);
    position: relative;
    z-index: 10;
    border-bottom: 2px solid var(--border-color);
}

.header h1 {
    font-size: 2.2rem; /* Etwas größer */
    font-weight: 700;  /* Dickere Schrift */
    text-align: center;
    flex: 1;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
    color: white;
}

.header-date {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.95);
    text-align: right;
    min-width: 220px;
    font-weight: 600;
}

.header-version {
    font-size: 0.9rem;
    color: #e0f2ff;
    font-weight: 700;
    margin-top: 0.2rem;
}

.hamburger-menu {
    display: none;
    background: none;
    border: none;
    color: white;
    font-size: 1.6rem;
    cursor: pointer;
    min-width: 45px;
}

/* Toolbar Styles */
.toolbar {
    grid-area: toolbar;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background-color: var(--card-bg);
    border-bottom: 2px solid var(--border-color);
    box-shadow: var(--shadow);
    position: relative;
    z-index: 5;
}

.toolbar-row {
    display: flex;
    gap: 0.75rem;
    flex-wrap: nowrap;
    justify-content: center;
    width: 100%;
}

.toolbar-row .button {
    height: 44px;
    min-width: 140px;
    display: flex;
    align-items: center;
    justify-content: center;
    white-space: nowrap;
    flex: 1;
    max-width: 220px;
    font-size: 1rem;
    font-weight: 600;
    border: 2px solid transparent;
}

/* Navigation Styles */
.nav {
    grid-area: nav;
    background-color: var(--sidebar-bg);
    color: var(--sidebar-text);
    padding: 1.75rem;
    overflow-y: auto;
    border-right: 2px solid var(--border-color);
    height: 100%;
}

.nav h3 {
    color: var(--sidebar-text);
    margin-bottom: 1.75rem;
    font-size: 1.3rem;
    font-weight: 700;
    padding-bottom: 0.75rem;
    border-bottom: 3px solid var(--primary-color);
}

.search-filter {
    margin-bottom: 2.25rem;
}

.search-input, .filter-select {
    width: 100%;
    padding: 0.875rem;
    margin-bottom: 1.25rem;
    border: 2px solid var(--border-color);
    border-radius: var(--border-radius);
    background-color: var(--card-bg);
    color: var(--text-color);
    font-size: var(--input-font-size);
    font-weight: 500;
    transition: var(--transition);
}

.search-input:focus, .filter-select:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.15);
}

.filter-group {
    margin-bottom: 1.25rem;
}

.filter-label {
    display: block;
    margin-bottom: 0.75rem;
    color: var(--sidebar-text);
    font-weight: 600;
    font-size: 1rem;
}

/* Students List */
.students-section {
    margin-top: 2.5rem;
}

.section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.25rem;
}

.section-header h4 {
    color: var(--sidebar-text);
    font-size: 1.1rem;
    font-weight: 700;
}

.student-count {
    background-color: var(--primary-color);
    color: white;
    padding: 0.375rem 0.75rem;
    border-radius: 16px;
    font-size: 0.9rem;
    font-weight: 600;
}

.students-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.student-item {
    display: flex;
    align-items: center;
    padding: 1.25rem;
    margin-bottom: 0.75rem;
    background-color: transparent;
    border-radius: var(--border-radius);
    cursor: pointer;
    transition: var(--transition);
    border: 2px solid transparent;
}

.student-item:hover {
    background-color: var(--sidebar-hover);
    border-color: var(--primary-color);
    transform: translateX(4px);
}

.student-item.selected {
    background-color: var(--primary-color);
    border-color: var(--primary-color);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.25);
}

.student-avatar {
    font-size: 1.4rem;
    margin-right: 0.875rem;
    flex-shrink: 0;
}

.student-info {
    flex: 1;
    min-width: 0;
}

.student-name {
    font-weight: 700;
    margin-bottom: 0.375rem;
    color: var(--sidebar-text);
    font-size: 1.05rem;
}

.student-details {
    font-size: 0.9rem;
    color: var(--sidebar-text);
    opacity: 0.9;
}

/* Navigation Footer */
.nav-footer {
    margin-top: 2.5rem;
    padding-top: 1.75rem;
    border-top: 2px solid var(--border-color);
}

.footer-section {
    margin-bottom: 1.75rem;
}

.footer-section h4 {
    color: var(--sidebar-text);
    margin-bottom: 0.875rem;
    font-size: 1rem;
    font-weight: 700;
}

.footer-section .button {
    width: 100%;
    margin-bottom: 0.75rem;
    text-align: left;
    padding: 0.875rem;
    justify-content: flex-start;
    font-weight: 600;
}

.app-info {
    text-align: center;
    margin-top: 2.5rem;
    padding-top: 1.25rem;
    border-top: 2px solid var(--border-color);
    color: var(--sidebar-text);
    font-size: 0.9rem;
    opacity: 0.8;
}

/* Main Content */
.main {
    grid-area: main;
    padding: 2.25rem;
    overflow-y: auto;
    background-color: var(--background-color);
    height: 100%;
}

.welcome-screen {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    text-align: center;
    color: var(--text-light);
}

.welcome-screen h2 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    color: var(--text-color);
    font-weight: 700;
}

.welcome-screen p {
    font-size: 1.2rem;
    max-width: 450px;
    line-height: 1.7;
    font-weight: 500;
}

/* Buttons */
.button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: var(--border-radius);
    cursor: pointer;
    font-size: var(--font-size);
    font-weight: 600;
    text-decoration: none;
    transition: var(--transition);
    background-color: var(--primary-color);
    color: white;
    gap: 0.75rem;
    height: 42px;
    border: 2px solid transparent;
}

.button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow);
}

.button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    transform: none;
}

.button-success {
    background-color: var(--success-color);
}

.button-danger {
    background-color: var(--danger-color);
}

.button-warning {
    background-color: var(--warning-color);
    color: white;
}

.button-info {
    background-color: var(--info-color);
}

/* Entry Cards */
.entry-card {
    background-color: var(--card-bg);
    border: 2px solid var(--border-color);
    border-radius: var(--border-radius);
    padding: 1.75rem;
    margin-bottom: 1.5rem;
    box-shadow: var(--shadow);
    transition: var(--transition);
}

.entry-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.18);
}

.entry-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.25rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--border-color);
}

.entry-header span:first-child {
    font-weight: 700;
    font-size: 1.2rem;
    color: var(--text-color);
}

.entry-header span:last-child {
    color: var(--text-light);
    font-size: 1rem;
    font-weight: 500;
}

.entry-card p {
    margin-bottom: 0.875rem;
    line-height: 1.6;
    font-weight: 500;
}

.entry-card strong {
    color: var(--text-color);
    font-weight: 700;
}

/* Modal Styles */
.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    padding: 1.25rem;
}

.modal {
    background-color: var(--card-bg);
    border-radius: var(--border-radius);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
    width: 100%;
    max-width: 650px;
    max-height: 90vh;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    border: 2px solid var(--border-color);
}

.modal-header {
    padding: 1.75rem;
    border-bottom: 2px solid var(--border-color);
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: var(--background-color);
}

.modal-header h2 {
    margin: 0;
    color: var(--text-color);
    font-size: 1.6rem;
    font-weight: 700;
}

.modal-close {
    background: none;
    border: none;
    font-size: 1.75rem;
    cursor: pointer;
    color: var(--text-color);
    font-weight: bold;
}

.modal-body {
    padding: 1.75rem;
    flex: 1;
    overflow-y: auto;
}

.modal-footer {
    padding: 1.25rem 1.75rem;
    border-top: 2px solid var(--border-color);
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: rgba(0, 0, 0, 0.03);
}

.version-info {
    font-size: 0.9rem;
    color: var(--text-color);
    font-weight: 500;
}

/* Settings Modal Specific */
.settings-section {
    margin-bottom: 2.5rem;
}

.settings-section h3 {
    margin-bottom: 1.25rem;
    color: var(--text-color);
    font-size: 1.3rem;
    font-weight: 700;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--border-color);
}

.theme-selector {
    display: flex;
    gap: 1.25rem;
    margin-bottom: 2rem;
}

.theme-option {
    flex: 1;
    text-align: center;
    cursor: pointer;
    padding: 1.25rem;
    border: 2px solid var(--border-color);
    border-radius: var(--border-radius);
    transition: var(--transition);
    background-color: var(--card-bg);
}

.theme-option:hover {
    border-color: var(--primary-color);
    transform: translateY(-2px);
}

.theme-option.selected {
    border-color: var(--primary-color);
    background-color: rgba(37, 99, 235, 0.12);
    box-shadow: 0 4px 15px rgba(37, 99, 235, 0.2);
}

.theme-option h4 {
    margin-bottom: 0.75rem;
    color: var(--text-color);
    font-weight: 700;
    font-size: 1.1rem;
}

.theme-option p {
    font-size: 0.9rem;
    color: var(--text-color);
    font-weight: 500;
}

.color-picker {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1.25rem;
    margin-top: 1.25rem;
}

.color-option {
    display: flex;
    flex-direction: column;
}

.color-option label {
    margin-bottom: 0.75rem;
    font-weight: 600;
    color: var(--text-color);
    font-size: 1rem;
}

.color-input {
    width: 100%;
    height: 45px;
    border: 2px solid var(--border-color);
    border-radius: var(--border-radius);
    padding: 0.375rem;
    cursor: pointer;
}

/* Responsive Design */
@media (max-width: 1200px) {
    :root {
        --sidebar-width: 260px;
    }
}

@media (max-width: 992px) {
    :root {
        --header-height: 75px;
        --toolbar-height: 90px;
        --sidebar-width: 240px;
    }
    
    .header h1 {
        font-size: 1.8rem;
    }
}

@media (max-width: 768px) {
    .app {
        grid-template-areas:
            "header"
            "toolbar"
            "main";
        grid-template-columns: 1fr;
        grid-template-rows: var(--header-height) var(--toolbar-height) 1fr;
    }

    .header {
        padding: 0 1.25rem;
    }

    .header h1 {
        font-size: 1.6rem;
        text-align: left;
        margin-left: 1rem;
    }

    .header-date {
        font-size: 0.9rem;
        min-width: 140px;
    }

    .nav {
        position: fixed;
        top: var(--header-height);
        left: -100%;
        width: 85%;
        max-width: 340px;
        height: calc(100% - var(--header-height));
        z-index: 1000;
        transition: left 0.3s ease;
        box-shadow: 3px 0 15px rgba(0, 0, 0, 0.15);
        background-color: var(--sidebar-bg) !important;
    }

    .nav.open {
        left: 0;
    }

    .hamburger-menu {
        display: block;
    }

    .toolbar {
        padding: 0.75rem;
        gap: 0.5rem;
    }

    .toolbar-row {
        gap: 0.5rem;
        flex-wrap: wrap;
        justify-content: flex-start;
    }

    .toolbar-row .button {
        min-width: auto;
        flex: 1;
        font-size: 0.9rem;
        padding: 0.625rem;
        max-width: none;
        font-weight: 600;
    }

    .main {
        padding: 1.25rem;
    }
    
    .modal {
        max-height: 85vh;
    }
    
    .theme-selector {
        flex-direction: column;
        gap: 1rem;
    }
}

@media (max-width: 576px) {
    :root {
        --header-height: 65px;
        --toolbar-height: auto;
        --font-size: 14px;
    }
    
    .header h1 {
        font-size: 1.4rem;
    }
    
    .toolbar {
        flex-direction: column;
        padding: 0.625rem;
    }
    
    .toolbar-row {
        flex-wrap: wrap;
    }
    
    .toolbar-row .button {
        min-width: calc(50% - 0.375rem);
        margin-bottom: 0.375rem;
        height: 40px;
    }
    
    .modal-header {
        padding: 1.25rem;
    }
    
    .modal-body {
        padding: 1.25rem;
    }
    
    .modal-footer {
        padding: 1rem 1.25rem;
        flex-direction: column;
        gap: 0.75rem;
    }
    
    .color-picker {
        grid-template-columns: 1fr;
    }
    
    .entry-card {
        padding: 1.25rem;
    }
    
    .welcome-screen h2 {
        font-size: 2rem;
    }
    
    .welcome-screen p {
        font-size: 1.1rem;
    }
}

/* iOS specific adjustments */
@supports (-webkit-touch-callout: none) {
    .modal {
        max-height: 80vh;
    }
    
    .nav {
        padding-bottom: constant(safe-area-inset-bottom);
        padding-bottom: env(safe-area-inset-bottom);
    }
}

/* High contrast support for accessibility */
@media (prefers-contrast: high) {
    .button {
        border: 2px solid currentColor;
    }
    
    .student-item {
        border: 2px solid currentColor;
    }
    
    .entry-card {
        border: 2px solid currentColor;
    }
    
    .modal {
        border: 2px solid currentColor;
    }
}

/* Reduced motion for accessibility */
@media (prefers-reduced-motion: reduce) {
    * {
        transition: none !important;
    }
    
    .button:hover {
        transform: none;
    }
    
    .entry-card:hover {
        transform: none;
    }
    
    .student-item:hover {
        transform: none;
    }
}

/* Verbesserte Scrollbars */
::-webkit-scrollbar {
    width: 10px;
}

::-webkit-scrollbar-track {
    background: var(--background-color);
    border-radius: 5px;
}

::-webkit-scrollbar-thumb {
    background: var(--primary-color);
    border-radius: 5px;
    border: 2px solid var(--background-color);
}

::-webkit-scrollbar-thumb:hover {
    background: var(--secondary-color);
}

/* Focus Styles für bessere Accessibility */
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
}

/* Verbesserte Lesbarkeit für lange Texte */
p, li, span {
    line-height: 1.7;
}

/* Hervorhebungen für wichtige Elemente */
.important {
    color: var(--danger-color);
    font-weight: 700;
}

.success {
    color: var(--success-color);
    font-weight: 700;
}

.warning {
    color: var(--warning-color);
    font-weight: 700;
}

/* Custom Color Overrides für das colored Theme */
[data-theme="colored"].custom-colors-applied .toolbar {
    /* Toolbar Hintergrund */
    background-color: var(--toolbar-bg-custom, var(--card-bg)) !important;
}

[data-theme="colored"].custom-colors-applied .modal {
    /* Modal Hintergründe */
    background-color: var(--modal-bg-custom, var(--card-bg)) !important;
}

[data-theme="colored"].custom-colors-applied .entry-card {
    /* Entry Cards Hintergründe */
    background-color: var(--card-bg, #ffffff) !important;
}

[data-theme="colored"].custom-colors-applied .main {
    /* Main Content Bereich */
    background-color: var(--background-color, #fef7ed) !important;
}

[data-theme="colored"].custom-colors-applied .nav {
    /* Navigation Sidebar */
    background-color: var(--sidebar-bg, #fed7aa) !important;
}

[data-theme="colored"].custom-colors-applied .header {
    /* Header */
    background: linear-gradient(135deg, 
        var(--primary-color, #dc2626), 
        var(--secondary-color, #b91c1c)
    ) !important;
}

/* Spezifische Anpassungen für bessere Lesbarkeit */
[data-theme="colored"].custom-colors-applied .nav h3,
[data-theme="colored"].custom-colors-applied .nav .filter-label,
[data-theme="colored"].custom-colors-applied .nav .student-name {
    color: var(--sidebar-text) !important;
}

/* Sicherstellen, dass Textfarben kontrastreich bleiben */
[data-theme="colored"].custom-colors-applied {
    --text-color: #1f2937;
    --text-light: #6b7280;
}

/* Responsive Anpassungen */
@media (max-width: 768px) {
    [data-theme="colored"].custom-colors-applied .nav {
        background-color: var(--sidebar-bg, #fed7aa) !important;
    }
}
